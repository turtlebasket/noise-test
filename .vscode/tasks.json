{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Copy to thinkpad",
            "type": "shell",
            "command": "GOOS=linux GOARCH=amd64 go build -o ppnode-bin ppnode/main.go && scp -r ppnode-bin thinkpad:/home/michael",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}